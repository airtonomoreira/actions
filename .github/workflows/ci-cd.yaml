name: Teste Workflow

on:
  push:
    branches:
      - main
jobs:
  test_job:
    name: Executando job test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Executando um comando simples
        run: echo "Teste de mensagem de aviso"
